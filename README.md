# ansible_playbook_infra_freeipa
Playbook for installing and managing FreeIPA
## Requirements

ansible-galaxy role install -r roles/requirements.yml

ansible-galaxy collection install -r collections/requirements.yml